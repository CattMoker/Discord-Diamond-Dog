class ContainerCapacity(Equipment):
    name = None
    capacity = None


    def _init_ContainerCapacity(self):
        name = ""
        capacity = ""

    def _init_ContainerCapacity(self, inName, inCapacity)
        Equipment._init_(inName, subName, cost, weight)

        self.name = inName
        self.capacity = inCapacity

    def setContainerCharacteristics(self, inName, inCapacity)
        self.setName(inName)
        self.setCapacity(inCapacity)

    def getContainerChars(self)
        print ("Name: " + self.getName())
        print ("Container Capacity: " + self.getCapacity())

    def getName(self)
        self.name = inName
    def getCapacity(self)
        self.capacity = inCapacity

    def setName(self, inName)
        self.name = inName
    def setCapacity(self, inCapacity)
        self.capacity = inCapacity



    def toString(self)
        self.getContainerChars()

    
